import { useEffect, useRef } from "react";


export const Main = () => {

    // const videoRef = useRef(null)
    // const localMedia = useRef(null)

    // useEffect(() => {
    //     let f = async () => {
    //         localMedia.current = await navigator.mediaDevices.getUserMedia({video:true, audio:true});

    //         videoRef.current.srcObject = localMedia.current
    //     }
    //     f()
    // }, [])

    return (
        <div>
            {/* <video ref={videoRef} autoPlay muted={false}></video> */}
            main
        </div>
    );
}